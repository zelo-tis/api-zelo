export enum GITHUB_REQUEST_EVENTS {
    PULL_REQUEST = 'PullRequestEvent'
}

export enum GITHUB_HOOK_EVENTS {
  PULL_REQUEST = 'pull_request'
}

export enum GIBHUB_ACTIONS {
  CLOSED = 'closed'
}

export enum REQUEST_STATUS {
  TODO = 'TODO',
  DOING = 'DOING',
  DONE = 'DONE'
}
